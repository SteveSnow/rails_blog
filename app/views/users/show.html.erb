<br>
<%=  @user.first_name %>
<br>
<% if session[:user_id] %>
	<% posts = @posts.reverse %>
	<% posts.each do |post| %>
		<%= post.message %>
		<br>
		<span class="datetime"><%= post.created_at.strftime("%I:%M%p" + ' ' + "%m/%d/%Y ") %></span>	
		<br>			 
	<% end %>
<% end %>

<% if session[:user_id] %>
	<% @users.each do |u|%>
		<%= u.first_name %>
		<% if @user.user_follows.where(follows_id:u.id).count!=0 %>
			**
			<% else %>

		<% end %>
		<br>
	<%end %>
<% end %>
